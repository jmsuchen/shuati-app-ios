# 刷题 App 开发说明

## 当前交付

已按 `项目方案-刷题App.md` 搭建原生 iOS SwiftUI 项目，工程入口为：

```text
刷题App.xcodeproj
```

MVP 已覆盖：

- 题库、练习、错题、统计、设置五个 Tab。
- markdown / txt 导入与清洗。
- docx 正文提取的基础实现。
- DeepSeek OpenAI 兼容接口接入，默认模型 `deepseek-v4-flash`。
- API Key 使用 Keychain 保存。
- 无 API Key 时使用本地规则生成基础填空题，便于先跑通流程。
- SwiftData 存储题库、题目、练习记录和错题。
- 答题即时判分，支持大小写、空格和常见中文标点标准化。
- 题库页只负责导入文档、手动录入材料并识别题目，不在题库页选择题量。
- 自动识别填空、四选一和判断题；四选一题会提供 4 个选项。
- 进入练习模块后选择本轮题量；结果页支持“重做一遍”和“重新出卷”。
- 练习模块可选择题型：填空、选择、判断、混合。
- 未完成练习会保存进度，再次进入同一题库时可选择继续或重新开始。
- 题库可在列表左滑删除，也可在详情页删除；删除时会同步清理未完成练习进度。
- 错题页支持重复练习；设置页可配置同一道错题连对几次后自动从错题本删除。
- 错题列表按题目去重展示；点进单题可单独练习、显示答案解析，也可单题删除。
- “重新出卷”会基于同一原文尽量更换挖空位置，避免每轮完全相同。
- 答错展示正确答案、用户答案、原文和解析。
- 结果页展示分数、正确数、错误数、正确率、平均用时和最长连对。
- “再练一次”可基于当前题库重新开始一轮练习。

## 本地运行

1. 用 Xcode 打开 `刷题App.xcodeproj`。
2. 选择 `刷题App` scheme 和 iOS 17+ 模拟器。
3. 如需真机运行，在 Signing & Capabilities 中选择你的 Team。
4. 启动后可先不配置 API Key，直接导入 `.md` 文件验证完整练习闭环。
5. API Key 使用单独本地配置文件：复制 `Config/APISecrets.example.xcconfig` 为 `Config/APISecrets.local.xcconfig` 后填写 `DEEPSEEK_API_KEY`。该本地文件已被 `.gitignore` 排除，不会上传 GitHub。设置页仍可填入用户自定义 Key 覆盖内置配置。

## 说明

已使用本机完整 Xcode 完成 generic iOS 编译检查：

```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -quiet -project 刷题App.xcodeproj -scheme 刷题App -configuration Debug -destination generic/platform=iOS -derivedDataPath ./DerivedData CODE_SIGNING_ALLOWED=NO build
```

当前环境的 CoreSimulatorService 在 Codex 沙箱中不可用，因此尚未启动模拟器运行。若要在 Xcode 图形界面或模拟器中运行，需要将开发目录切到完整 Xcode：

```bash
sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
```

这一步需要输入本机管理员密码。
